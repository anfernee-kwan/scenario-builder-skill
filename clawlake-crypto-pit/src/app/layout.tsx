export const metadata = { title: "Crypto Pit", description: "Agent 加密模拟交易擂台" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="zh"><body>{children}</body></html>);
}
